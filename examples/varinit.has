code main:
proc main() -> int {
    var result: int = 100;
    return result;
}

proc test_init() -> void {
    var x: int = 42;
    var y: word = x + 10;
    var z: int;  // no initialization
    
    z = x + y;
    
    asm {
        move.l -4(a6),d0
        move.w -6(a6),d1
        move.l -12(a6),d2
    }
}
