; Generated SIMD operations template
; Variables available: operations (list), count (int)

code simd_generated:
{% for op in operations %}
    proc simd_{{ op }}_vectors() -> int {
        var result:int = 0;
        var i:int = 0;
        var v0:int = 10;
        var v1:int = 20;
        var v2:int = 30;
        
        {% if op == "add" %}
            result = v0 + v1 + v2;
        {% elif op == "sub" %}
            result = v0 - v1 - v2;
        {% elif op == "mul" %}
            result = v0 * v1;
        {% elif op == "div" %}
            if (v1 != 0) {
                result = v0 / v1;
            }
        {% endif %}
        
        return result;
    }
{% endfor %}

    ; Common handler procedure
    proc simd_sum_all() -> int {
        var total:int = 0;
        repeat {{ count }} {
            total = total + 1;
        }
        return total;
    }
